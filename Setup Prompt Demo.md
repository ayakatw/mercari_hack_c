# コードエージェント向け: 開発環境セットアップ + コードベース規約

---

## 役割

このリポジトリの開発環境を 0 から構築し、あわせて**コードベース規約を `CLAUDE.md` として確立**します。

**プロダクト機能は実装しません。** 仕様（spec）は別途用意します。この作業のゴールは「spec ができた瞬間に、迷いなく実装を始められる土台と規約が揃っている」ことです。

---

## 背景

推し活 SNS アプリ。ホーム（フィード）/ 探す / 作成・出品 / トーク / マイページ の 5 タブ構成のモバイルアプリ。投稿画像に付いたアイテムタグからメルカリへ遷移する導線を持つ。

**当面の目的はデモです。** 動く画面を見せることが最優先で、ロジックは組めた範囲で構いません。したがって:

- 認証は作らない。ダミーユーザーを固定し、切り替えられるようにする
- 堅牢性より、動く画面が早く揃うことを優先する
- ただし**後から堅牢にできる形**は保つ。特にクエリの引数の形（後述 R3）

4 人チーム。実装は主に AI エージェントが行います。

---

## 技術選定（決定事項）

再検討や代替案の提示は不要です。

| 層 | 選定 |
|---|---|
| リポジトリ | monorepo（pnpm workspace） |
| 言語 | TypeScript のみ。**Python は使わない** |
| アプリ | Expo + expo-router |
| バックエンド | Hono |
| DB | PostgreSQL + Drizzle ORM |
| 認証 | **なし**（ダミーユーザー） |
| データ取得 | TanStack Query |
| テスト | Vitest |
| 品質 | Biome + `tsc --noEmit`(strict) |
| ランタイム固定 | mise |

**TypeScript に統一する目的は、Hono RPC で server とアプリの型を直結させることです。** openapi や orval によるコード生成は使いません。ルート定義を変えたらアプリ側がその場でコンパイルエラーになる状態を作ってください。

---

## ディレクトリ構成

```
oshikatsu/
├── app/                    # Expo（モバイルアプリ）
│   └── src/
│       ├── app/            # 画面（expo-router）
│       ├── features/       # 画面別ロジック
│       ├── components/     # 共通 UI
│       └── lib/api.ts      # server の型を import
├── server/                 # Hono（API）
│   └── src/
│       ├── index.ts
│       ├── routes/         # health.ts, posts.ts, ...
│       ├── db/
│       │   ├── schema.ts   # Drizzle。単一の source
│       │   ├── queries/    # クエリ関数
│       │   └── seed.ts
│       ├── lib/            # viewer / pagination / errors
│       └── tests/
├── docker-compose.yml      # postgres のみ
├── package.json
└── CLAUDE.md
```

**この構成を勝手に深くしないこと。** `domain/` や `usecase/` などのレイヤを足す提案は不要です。ディレクトリが浅いのは意図的な選択です。

---

# コードベース規約

以下は決定事項です。Phase H で `CLAUDE.md` に落とし込みます。**規約は書くだけでは守られないので、機械で強制できるものは設定に落とし、残りだけ文章にする**という原則で書き分けてください。

## R1. ファイルの責務

| 場所 | やること | 書かないこと |
|---|---|---|
| `routes/*.ts` | パス定義、入力バリデーション、クエリ関数の呼び出し | 生の SQL、業務判断の分岐 |
| `db/schema.ts` | テーブル定義、リレーション、インデックス | ロジック |
| `db/queries/*.ts` | クエリの実装 | HTTP の知識、レスポンス整形 |
| `lib/*.ts` | viewer 解決・ページネーション・エラー整形 | 個別機能の都合 |
| `app/src/app/**` | 画面のレイアウトと合成 | データ取得の実体、業務判断 |
| `app/src/features/**` | 画面別のフック・状態・表示ロジック | 他 feature からの import |

## R2. 依存の向き

- `app/src/features/A` は `app/src/features/B` を import しない。共有したくなったら `components/` か `lib/` に上げる
- `routes/` は `db/queries/` を呼ぶ。`db/queries/` は `routes/` を知らない
- アプリは server の**型のみ** import する

## R3. viewer を必ず引数に取る

`db/queries/` の**すべての関数は、第一引数に `viewer` を受け取る**こと。

デモ段階では中身を使わなくて構いません。**引数の形だけ守ってください。**

理由: 本番ではフィード・探す・トーク・投稿詳細のすべてのクエリに、ブロックと公開範囲の条件が必要になります。各所で `if` を書く運用にすると必ず漏れ、「ブロックした相手の投稿が見える」という最悪の事故になります。引数さえ揃っていれば、後から `lib/visibility.ts` を作って中身を入れるだけで済みます。逆に今省くと、全クエリのシグネチャを直すことになります。**コストはほぼゼロで、後の効きが大きい部分です。**

## R4. ページネーションとエラー

Hono は薄く、これらの作法が用意されていません。**共通の入口を `lib/` に用意し、必ずそれを通す**こと。ルートごとに独自実装を書かない。

- ページネーション: カーソル方式に統一。offset は使わない。SNS のフィードは投稿が増減するのでページがズレる
- エラー: 例外 → HTTP レスポンスの変換を 1 箇所に。ルート内で `c.json({ error })` を直接組み立てない

## R5. 命名

- ファイル名は kebab-case
- テーブル・カラムは snake_case、TS 側は camelCase（Drizzle のマッピングで吸収）
- 真偽値は `is` / `has` で始める
- 日時は UTC 保存、カラム名を `*_at` で揃える

## R6. テスト

デモ優先なので網羅は求めません。ただし以下は守ること。

- **DB をモックしない。** ローカルの postgres に接続してテストする。モックするとクエリのバグ・N+1・制約違反が全部すり抜ける
- ルートのテストは `app.request()` によるインプロセス実行。サーバーを起動しない
- **テストは速く決定的であること。** 遅いとエージェントが実行せずに「通るはず」と報告し始める
- カバレッジ率を目標にしない
- スナップショットテストは使わない。差分が出たら気軽に更新されて意味を失う

## R7. 禁止

- `any` / `@ts-ignore` / `@ts-expect-error`（理由コメント付きの例外のみ）
- テストの `skip` / `only` のコミット
- `console.log` の残置
- lockfile を更新せずライブラリを追加すること
- 生成物・`node_modules`・`.env` のコミット

## R8. Git

- ブランチ: `feat/` `fix/` `chore/` + 短い説明
- コミットは Conventional Commits。スコープを付ける（`feat(server):` / `feat(app):`）
- **PR は 1 つの関心事に絞る。** スキーマ変更と画面実装を混ぜない

---

## スコープ

### やること

ランタイム固定、monorepo 骨格、docker compose（postgres のみ）、Drizzle スキーマ、Hono の骨組みと `lib/` の共通入口、Expo の骨組み、Hono RPC の型接続、**デモ用ユーザー切り替え**、テスト基盤、シード、CI、`CLAUDE.md` と skill。

### やらないこと

- プロダクト機能の実装（フィード、投稿、トーク、メルカリ連携）
- 認証
- 本番デプロイ、IaC、監視
- Redis、S3、worker プロセス
- 依頼していないライブラリ・抽象化・レイヤ

**サンプルとして作るルートは `health` と `posts`（一覧のみ）の 2 本だけ。**

---

## 着手前に必ず質問すること

**推測で決めず、作業開始前にまとめて質問し、回答を得てから着手してください。**

1. Node のバージョン（LTS でよいか）
2. リポジトリ名と GitHub の配置先
3. iOS / Android のどちらを先に動作確認するか

---

## 作業手順

**各フェーズの完了条件を満たしてから次へ進み、フェーズごとに一度手を止めて報告してください。**

### Phase A — ランタイム固定と monorepo 骨格

`.mise.toml`、`pnpm-workspace.yaml`、ルート `package.json`、`.gitignore` / `.editorconfig` / `.gitattributes` / `.env.example`、Biome 設定をルートに 1 つ。

**完了条件**: クリーンな環境で `mise install && pnpm install` が通る。

### Phase B — DB とスキーマ

`docker-compose.yml` に postgres のみ（`healthcheck` を書く）。Drizzle の設定と `db/schema.ts`。テーブルは `users` / `posts` の 2 つだけ。R5 の命名に従うこと。

**完了条件**: `pnpm db:push` でスキーマが適用され、テーブルの存在を確認できる。

### Phase C — server の骨組みと共通入口

- Hono の起動、`/health` ルート
- **`lib/` に viewer 解決・ページネーション・エラー整形の入口を用意する（R3・R4）**

  後から作ると、既にバラバラの実装が積み上がった状態になります。**このフェーズで必ず作ってください。**
- `db/queries/posts.ts` に一覧クエリ。**viewer を第一引数にする**
- `routes/posts.ts` に一覧エンドポイント 1 本
- Hono RPC の型をエクスポート

**完了条件**: `curl localhost:3000/health` が 200。`/posts` が一覧を返す。

### Phase D — デモ用ユーザー切り替え

デモでは「A さんから見た画面」「B さんから見た画面」を見せる場面が必ず来ます。認証がない今の方がむしろ簡単に作れるので、ここで用意します。

- リクエストヘッダ（例: `X-Demo-User`）で viewer を切り替える
- ヘッダが無ければシードの既定ユーザーを使う
- `lib/` の viewer 解決関数がこれを担う

**完了条件**: ヘッダを変えると `/posts` の結果が変わることを確認できる。

### Phase E — テスト基盤

Vitest。ローカル postgres に接続。`app.request()` によるインプロセステストで `/health` と `/posts` のテストを書く。R6 に従うこと。

**完了条件**: `pnpm test` が通る。実行時間を報告に含める。

### Phase F — アプリの骨組み

Expo + expo-router、5 タブのタブバー（中身は空でよい）、`lib/api.ts` で Hono RPC クライアント、TanStack Query の Provider、`/posts` を叩いて一覧を表示。

デバッグメニューかヘッダ設定で**デモユーザーを切り替えられる**ようにすること。

**実機から `localhost` は見えません。** 開発時は PC の LAN IP を環境変数で渡す必要があります。新メンバーが必ず一度詰まるので README に明記すること。

**完了条件**: 実機の Expo Go で 5 タブが表示され、`/posts` の結果が画面に出る。ユーザー切り替えが効く。

### Phase G — シードデータ

**デモの説得力はシードの質でほぼ決まります。ここだけは手を抜かないでください。** 空の画面はデモになりません。

- ユーザー 30 名程度。推し棚が偏った分布にする
- フォロー関係、投稿 200 件程度、アイテムタグ付きの投稿を含む
- **投稿画像はグレーの四角にしない。** 推し活アプリとして成立する見た目にすること。フリー素材の URL でも、生成した画像でもよい
- ユーザー名・キャプション・価格は実在しそうな具体値にする。Lorem ipsum 禁止
- seed を固定して**冪等**にする

**完了条件**: `pnpm seed` を 2 回実行して件数が変わらない。アプリの一覧画面が、デモとして見せられる見た目になっている。

### Phase H — 型接続の検証

**この構成の核心なので必ず実地で確認すること。**

`routes/posts.ts` のレスポンス型を変更し、アプリ側で `pnpm typecheck` がコンパイルエラーになることを確認する。確認後、変更を戻す。

**完了条件**: 型エラーの実際の出力を報告に含める。

### Phase I — CI と規約

CI は `lint` / `typecheck` / `test` の 3 ジョブ。`pnpm install --frozen-lockfile` を使う。

規約を `CLAUDE.md` に落とし込む。**丸写しではなく、機械で強制できるものは設定に落とし、残りだけ文章にすること。**

- ルート `CLAUDE.md`: ディレクトリ責務、依存の向き、禁止事項、コマンド一覧、Git 運用。**短く保つ。読まれない量になった時点で価値がゼロになります**
- `server/CLAUDE.md`: R3・R4・R6 を中心に
- `app/CLAUDE.md`: R1・R2 のアプリ側を中心に
- skill を 2 本:
  - `add-route`: `lib/` の共通入口の使い方、viewer 必須クエリ、テストの書き方
  - `add-screen`: expo-router の画面追加手順

**完了条件**: PR を 1 本作り、全ジョブが緑になる。`add-route` に従って自分でダミールートを追加し、手順に漏れがないことを検証する（検証後は削除）。

---

## 禁止事項

- **未確定事項を推測で埋めない。** 迷ったら止まって質問する
- **動いていないのに動いたと報告しない。** 完了条件のコマンドを実際に実行し、その出力を報告に含める
- 通らないテストを skip / `@ts-ignore` で隠さない
- Python、openapi、コード生成パイプラインを持ち込まない
- 認証を実装しない
- 依頼していないライブラリ・抽象化・レイヤを足さない
- プロダクト機能を実装しない
- `CLAUDE.md` や skill を冗長にしない

---

## 報告フォーマット

各フェーズ完了時に:

1. 作成・変更したファイルの一覧
2. 完了条件のコマンドと、その実行結果（出力を貼る）
3. 判断に迷った点と、どう決めたか
4. 次フェーズに進んでよいかの確認
