{
  "name": "oshiport-demo",
  "version": "1.0.0",
  "private": true,
  "description": "推しポート デモ — 静的フロント配信 + Gemini 画像解析 API",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
